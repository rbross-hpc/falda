node_modules/
dist/
*.db
*.db-wal
*.db-shm
falda-blobs/
example-blobs/
example.db*
*.log
.DS_Store
__pycache__/
falda-data/
*.bak-premerge-*
*.tenant-aware.py
opencode.json
.opencode/

# FALDA access proxy secrets (never commit)
proxy/falda_proxy_tokens.json
proxy/falda_proxy.crt
proxy/falda_proxy.key
proxy/proxy.log

# FALDA canonical token file (never commit) — shared by HTTP API and MCP
# when run via `falda serve`.
falda_tokens.json

# Legacy per-surface token files (never commit) — still honored by the
# standalone falda_gateway.js / falda_mcp.js entry points.
falda_mcp_tokens.json
falda_gateway_tokens.json
